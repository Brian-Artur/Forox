# Defer
## 🧠 ¿Qué hace `defer`?

El atributo `defer` en una etiqueta `<script>` le **dice al navegador**:

> _"Descarga este script en segundo plano mientras se sigue procesando el HTML, y **ejecútalo sólo después de que todo el HTML haya sido parseado** (es decir, cuando el DOM esté listo)"_.

